# Memory Index

Structured project knowledge for Hickory. Load only the file matching the current task — never all, never none.

| File | Content | When to load |
|------|---------|---------------|
| [deployment.md](deployment.md) | Release process, CI/CD workflows, in-app update mechanism | For deploy/release/CI topics |
| [features.md](features.md) | Feature index — links to per-feature detail files | To find the right feature file |

See the root `CLAUDE.md` "Context Memory System" section for the full protocol (when to load, when to update, file structure conventions).
